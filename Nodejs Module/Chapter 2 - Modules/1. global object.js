// Below are the global objects


console.log(); // global object



setTimeout(); // to call a function after a delay

clearTimeout();


setInterval(); // To call a function repeatedly

clearInterval(); // 


window.console.log() // DOM global object

window.setTimeout();

// In node we have global as the global object.
// So in node var let or const will not defined as the global object. They will only be scoped within this fileName.js